{% extends "base.html.twig" %}

{% block title %}Films
{% endblock %}

{% block body %}
	<div>

		<div>Voici la liste des Films disponibles :</div>

		{% if films | length == 0 %}
			pas de films disponibles

		{% else %}
			<ul>
				{% for film in films %}
					<li>{{film.Title}}</li>
					<a href="{{path('updateF', {'id':film.id} )}} ">Editer</a>
					<a href="{{path('deleteF', {'id':film.id} )}} ">Supprimer</a>
				{% endfor %}
			</ul>

		{% endif %}


	</div>
{% endblock %}
